version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm ci || npm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
